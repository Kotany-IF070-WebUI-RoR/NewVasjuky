=link_to_if(Category.calculate(id, 'opened') != 0, Category.calculate(id, 'opened'), account_admin_category_path(id: id, status: 'opened'), title: 'Відкриті')
|  | 
=link_to_if(Category.calculate(id, 'closed') != 0, Category.calculate(id, 'closed'), account_admin_category_path(id: id, status: 'closed'), title: 'Закриті')
|  | 
=link_to_if(Category.calculate(id, 'pending') != 0, Category.calculate(id, 'pending'), account_admin_category_path(id: id, status: 'pending'), title: 'На модерації')
|  | 
=link_to_if(Category.calculate(id, 'declined') != 0, Category.calculate(id, 'declined'), account_admin_category_path(id: id, status: 'declined'), title: 'Відхилені')