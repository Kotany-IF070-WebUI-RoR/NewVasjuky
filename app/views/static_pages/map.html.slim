link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/0.7.7/leaflet.css"
script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/0.7.7/leaflet.js"
script src="http://www.matchingnotes.com/javascripts/leaflet-google.js"

h1 StaticPages#map
p Find me in app/views/static_pages/map.html.slim
#issues-map

javascript: 
  var map = new L.Map('issues-map', 
            { center: new L.LatLng(50.1192478, 26.8197633), 
              zoom: 9});
  var googleLayer = new L.Google('ROADMAP');
  map.addLayer(googleLayer);

  var markers = [];
  - @issues.each do |i|
    addMarker(#{{i.geocode}})

  function addMarker(coords) {
    var marker = L.marker(coords).addTo(map);
    markers.push(coords)
  }

  var bounds = new L.LatLngBounds(markers);
  map.fitBounds(bounds);

